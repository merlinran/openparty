<div class="pending_event">
  <%= image_tag(event.poster_url) %>
  <div class="event_brief">
    <h1><%= link_to event.name, event %></h1>
    <p><%= event.hold_date %></p>
    <p><%= event.location %></p>
    <p><%= truncate(event.detail, length: 80, seperator: ' ') %></p>
  </div>
</div>
