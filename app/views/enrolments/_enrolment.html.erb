<% user = enrolment.user %>
<li class="enrolment <%= "current_user_enrolment" if user == current_user %>">
  <%= image_tag user.avatar_url, alt: user.name, class: "avatar" %>
  <br/><span><%= user.name %></span>
</li>

